# Local pack-only — used by deploy/pack-local.sh (not for production secrets).
# Copy: cp .env.pack.example .env.pack

POSTGRES_USER=timia
POSTGRES_PASSWORD=pack-only-not-used
POSTGRES_DB=timia
NEXT_PUBLIC_API_BASE_URL=https://timia.online/api
