# Timia MVP-1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 monorepo（同目录）内交付一个可本地运行的 Next.js + FastAPI + Postgres 应用，支持登录、Workspace/Project/Item/Comment 基础功能，以及 Workspace 成员的手动管理与活动流记录。

**Architecture:** Monorepo 结构 `codes/web`（Next.js）与 `codes/api`（FastAPI/uv），通过 REST API 通信。权限以 Workspace 成员+角色为核心，后端强制校验；活动流写入 `activity_log`。第一期不做实时推送。

**Tech Stack:** Next.js, React, TypeScript, Tailwind, shadcn/ui, TanStack Query, FastAPI, SQLAlchemy 2.0, Alembic, Postgres, uv, pytest

---

## File Structure (to be created)

- Create: `codes/api/pyproject.toml`
- Create: `codes/api/app/main.py`
- Create: `codes/api/app/core/config.py`
- Create: `codes/api/app/core/security.py`
- Create: `codes/api/app/db/session.py`
- Create: `codes/api/app/db/base.py`
- Create: `codes/api/app/models/*.py`
- Create: `codes/api/app/schemas/*.py`
- Create: `codes/api/app/routes/*.py`
- Create: `codes/api/app/services/activity.py`
- Create: `codes/api/app/migrations/` (Alembic)
- Create: `codes/api/tests/*.py`

- Create: `codes/web/package.json`
- Create: `codes/web/next.config.*`
- Create: `codes/web/app/(auth)/login/page.tsx`
- Create: `codes/web/app/w/[workspaceId]/...` pages
- Create: `codes/web/src/lib/api.ts`
- Create: `codes/web/src/lib/auth.ts`
- Create: `codes/web/src/components/...`

- Create: `docker-compose.yml` (Postgres)
- Create: `Makefile`
- Create: `.env.example`

---

### Task 1: Bootstrap monorepo + local dev orchestration

**Files:**
- Create: `docker-compose.yml`
- Create: `Makefile`
- Create: `.env.example`
- Create: `README.md`

- [ ] **Step 1: Create `docker-compose.yml` for Postgres**

```yaml
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: timia
      POSTGRES_PASSWORD: timia
      POSTGRES_DB: timia
    ports:
      - "5432:5432"
    volumes:
      - timia_pgdata:/var/lib/postgresql/data
volumes:
  timia_pgdata:
```

- [ ] **Step 2: Create `Makefile` to start db/api/web**

```makefile
.PHONY: dev db api web api-install web-install

dev: db api web

db:
	docker compose up -d db

api-install:
	cd codes/api && uv sync

api:
	cd codes/api && uv run uvicorn app.main:app --reload --port 8000

web-install:
	cd codes/web && npm install

web:
	cd codes/web && npm run dev -- --port 3000
```

- [ ] **Step 3: Add `.env.example`**

```bash
# codes/api/.env
DATABASE_URL=postgresql+psycopg://timia:timia@localhost:5432/timia
JWT_SECRET=dev_secret_change_me
JWT_ISSUER=timia
JWT_AUDIENCE=timia-web
ACCESS_TOKEN_EXPIRES_MINUTES=30
REFRESH_TOKEN_EXPIRES_DAYS=14

# codes/web/.env.local
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
```

- [ ] **Step 4: Write `README.md` with dev steps**

```md
## Dev
1) `make db`
2) `make api-install && make api`
3) `make web-install && make web`
```

- [ ] **Step 5: Commit**

```bash
git init
git add .
git commit -m "chore: bootstrap timia monorepo structure"
```

---

### Task 2: FastAPI app skeleton + config + health

**Files:**
- Create: `codes/api/pyproject.toml`
- Create: `codes/api/app/main.py`
- Create: `codes/api/app/core/config.py`

- [ ] **Step 1: Create `codes/api/pyproject.toml` (uv)**

```toml
[project]
name = "timia-api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2.6",
  "pydantic-settings>=2.2",
  "sqlalchemy>=2.0",
  "alembic>=1.13",
  "psycopg[binary]>=3.1",
  "python-jose[cryptography]>=3.3",
  "passlib[bcrypt]>=1.7",
]

[tool.uv]
dev-dependencies = [
  "pytest>=8.0",
  "httpx>=0.27",
  "ruff>=0.5",
]
```

- [ ] **Step 2: Create config**

```python
# codes/api/app/core/config.py
from pydantic_settings import BaseSettings, SettingsConfigDict

class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", extra="ignore")
    database_url: str
    jwt_secret: str
    jwt_issuer: str = "timia"
    jwt_audience: str = "timia-web"
    access_token_expires_minutes: int = 30
    refresh_token_expires_days: int = 14

settings = Settings()
```

- [ ] **Step 3: Create minimal app with health route**

```python
# codes/api/app/main.py
from fastapi import FastAPI

app = FastAPI(title="Timia API")

@app.get("/health")
def health():
    return {"ok": True}
```

- [ ] **Step 4: Run**

Run: `cd codes/api && uv sync && uv run uvicorn app.main:app --reload --port 8000`
Expected: `GET http://localhost:8000/health` returns `{"ok": true}`

- [ ] **Step 5: Commit**

```bash
git add codes/api
git commit -m "feat(api): add FastAPI skeleton and config"
```

---

### Task 3: Database layer + models + Alembic migration

**Files:**
- Create: `codes/api/app/db/session.py`
- Create: `codes/api/app/db/base.py`
- Create: `codes/api/app/models/{user,workspace,project,item,comment,activity}.py`
- Create: `codes/api/alembic.ini`
- Create: `codes/api/app/migrations/*`

- [ ] **Step 1: Create SQLAlchemy engine/session**

```python
# codes/api/app/db/session.py
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from app.core.config import settings

engine = create_engine(settings.database_url, pool_pre_ping=True)
SessionLocal = sessionmaker(bind=engine, autoflush=False, autocommit=False)
```

- [ ] **Step 2: Create declarative Base**

```python
# codes/api/app/db/base.py
from sqlalchemy.orm import DeclarativeBase

class Base(DeclarativeBase):
    pass
```

- [ ] **Step 3: Add models**

Create models with UUID PK, timestamps, and relations matching spec:
- User(email unique, password_hash, display_name)
- Workspace(created_by_user_id)
- WorkspaceMember(unique workspace_id+user_id, role, status)
- Project(workspace_id, archived)
- Item(project_id, workspace_id, title, body, status, priority, due_at, version)
- Comment(item_id, workspace_id, author_user_id, body, deleted_at)
- ActivityLog(workspace_id, actor_user_id, entity_type, entity_id, action, metadata json)

- [ ] **Step 4: Setup Alembic and generate initial migration**

Run:
- `cd codes/api && uv run alembic init app/migrations`
- Configure env.py to import Base + models and use `settings.database_url`
- `uv run alembic revision --autogenerate -m "init schema"`
- `uv run alembic upgrade head`

Expected: tables created in Postgres.

- [ ] **Step 5: Commit**

```bash
git add codes/api
git commit -m "feat(api): add DB models and initial migrations"
```

---

### Task 4: Auth (login, refresh, me) + password hashing

**Files:**
- Create: `codes/api/app/core/security.py`
- Create: `codes/api/app/routes/auth.py`
- Modify: `codes/api/app/main.py`
- Test: `codes/api/tests/test_auth.py`

- [ ] **Step 1: Implement hashing + JWT helpers**

```python
# codes/api/app/core/security.py
from datetime import datetime, timedelta, timezone
from jose import jwt
from passlib.context import CryptContext
from app.core.config import settings

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

def hash_password(p: str) -> str:
    return pwd_context.hash(p)

def verify_password(p: str, h: str) -> bool:
    return pwd_context.verify(p, h)

def create_access_token(subject: str) -> str:
    now = datetime.now(timezone.utc)
    exp = now + timedelta(minutes=settings.access_token_expires_minutes)
    return jwt.encode(
        {"sub": subject, "iss": settings.jwt_issuer, "aud": settings.jwt_audience, "iat": int(now.timestamp()), "exp": int(exp.timestamp())},
        settings.jwt_secret,
        algorithm="HS256",
    )
```

- [ ] **Step 2: Create auth routes (login/me)**
- Login: verify user, return access token; set refresh cookie (or skip refresh in MVP and keep access only)
- Me: Authorization: Bearer access

- [ ] **Step 3: Add dependency `get_current_user`**
- Parse Bearer token, validate `iss/aud/exp`, load user, reject disabled.

- [ ] **Step 4: Tests**

```python
def test_login_and_me_flow():
    ...
```

- [ ] **Step 5: Commit**

```bash
git add codes/api
git commit -m "feat(api): add JWT auth endpoints"
```

---

### Task 5: Workspace + manual member management + enforcement

**Files:**
- Create: `codes/api/app/routes/workspaces.py`
- Create: `codes/api/app/routes/members.py`
- Create: `codes/api/app/services/authorization.py`
- Test: `codes/api/tests/test_membership.py`

- [ ] **Step 1: Authorization helpers**
- `require_workspace_member(workspace_id)`
- `require_workspace_role(workspace_id, allowed_roles=[...])`

- [ ] **Step 2: Implement endpoints**
- List workspaces for current user
- Create workspace (creator becomes owner)
- List members
- Add member by email
- Change role
- Remove member (soft remove or delete row)

- [ ] **Step 3: Tests for role gates**

- [ ] **Step 4: Commit**

```bash
git add codes/api
git commit -m "feat(api): add workspace membership management"
```

---

### Task 6: Projects + Items + Comments + ActivityLog writing

**Files:**
- Create: `codes/api/app/services/activity.py`
- Create: `codes/api/app/routes/projects.py`
- Create: `codes/api/app/routes/items.py`
- Create: `codes/api/app/routes/comments.py`
- Create: `codes/api/app/routes/activity.py`
- Test: `codes/api/tests/test_crud_and_activity.py`

- [ ] **Step 1: ActivityLog service**
- `log_activity(workspace_id, actor_user_id, entity_type, entity_id, action, metadata)`

- [ ] **Step 2: CRUD endpoints and ensure activity is logged**
- For create/update/delete project/item/comment and member changes

- [ ] **Step 3: Item optimistic lock**
- PATCH must include `version`; if mismatch return 409

- [ ] **Step 4: Tests**
- Verify activity rows written
- Verify 409 on version mismatch

- [ ] **Step 5: Commit**

```bash
git add codes/api
git commit -m "feat(api): add projects/items/comments with activity log"
```

---

### Task 7: Web app skeleton + login + workspace navigation

**Files:**
- Create: `codes/web/package.json`
- Create: `codes/web/app/*`
- Create: `codes/web/src/lib/api.ts`

- [ ] **Step 1: Scaffold Next.js in `codes/web`**
Run: `cd codes/web && npm create next-app@latest . -- --ts --eslint --app --tailwind`

- [ ] **Step 2: Add API client**
- base URL from `NEXT_PUBLIC_API_BASE_URL`
- attach access token (initially store in memory/localStorage; later move HttpOnly cookie)

- [ ] **Step 3: Login page**
- Form -> call `/auth/login` -> store token -> redirect to workspace list

- [ ] **Step 4: Workspace list page**
- Call `/workspaces`

- [ ] **Step 5: Commit**

```bash
git add codes/web
git commit -m "feat(web): add Next.js skeleton and auth pages"
```

---

### Task 8: Web CRUD pages (projects/items/comments) + members admin page

**Files:**
- Create/Modify: pages under `codes/web/app/w/[workspaceId]/...`
- Create: basic components for lists/forms

- [ ] **Step 1: Projects list + create/edit**
- [ ] **Step 2: Items list + create/edit + detail**
- [ ] **Step 3: Comments list + add/delete**
- [ ] **Step 4: Members settings page (owner/admin only)**

- [ ] **Step 5: Commit**

```bash
git add codes/web
git commit -m "feat(web): add workspace CRUD UI and member management"
```

---

## Plan Self-Review

- Spec coverage: 登录、workspace、成员手动管理、project/item/comment、activity、乐观锁均覆盖。
- Placeholder scan: 若后续实现 refresh token 复杂，可先 MVP 仅 access token + 重新登录。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-04-23-timia-mvp1.md`. Two execution options:

1. **Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration
2. **Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

Which approach?

