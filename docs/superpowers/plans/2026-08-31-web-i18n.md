# Web 中英双语（第一期）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 Timia Web 加上 cookie 驱动的中英界面语言，URL 不变；第一期覆盖脚手架、壳层、登录和注册。

**Architecture:** `next-intl` 无 `[locale]` 路由。`getRequestConfig` 读 cookie `locale`（非法或缺省 → `zh`），加载 `messages/{zh|en}.json`。根 layout 提供 `NextIntlClientProvider` 与动态 `html lang`。切换走 server action 写 cookie 再 `router.refresh()`。客户端用 `useTranslations`。

**Tech Stack:** Next.js App Router、`next-intl`、现有 Tailwind / `"use client"` 组件；locale 纯函数用 Node `node:test` 覆盖。

## Global Constraints

- Spec: `docs/superpowers/specs/2026-08-31-web-i18n-design.md`
- Locales: `zh` | `en`；default `zh`；**不**读 `Accept-Language`
- URL 无 `/zh`、`/en` 前缀；不把 `app/` 改成 `app/[locale]/...`；不新增 `middleware.ts`
- Cookie 名 `locale`，Path `/`，Max-Age `31536000`，SameSite `Lax`，HttpOnly `false`，Secure 仅 production
- `<html lang>`：`zh` → `zh-CN`，`en` → `en`
- 品牌名两套文案都是 `Timia`
- 不改 core-service、iOS、`users` 表；不迁日程/工作空间/规划/健康等内容页
- 工作空间名、项目名、显示名等用户数据不进 json
- 后端错误码映射到 `auth.*` key；禁止「响应含汉字就原样展示」
- 语言名「中文」「English」两套 json 值相同
- 过渡期混排是预期：英文导航 + 中文内容页
- 不引入 `.ts` 扩展名的 production import
- 用户未要求时不要 git commit

---

## File map

| File | Role |
|------|------|
| `codes/web/src/i18n/config.ts` | **New** — locale 常量、`resolveLocale`、`htmlLang`、cookie 名 |
| `codes/web/src/i18n/config.test.ts` | **New** — `resolveLocale` / `htmlLang` |
| `codes/web/src/i18n/routing.ts` | **New** — `defineRouting`（`localePrefix: 'never'`） |
| `codes/web/src/i18n/request.ts` | **New** — `getRequestConfig` |
| `codes/web/src/i18n/actions.ts` | **New** — `changeLocaleAction` |
| `codes/web/src/i18n/next-intl.d.ts` | **New** — `AppConfig` 类型 |
| `codes/web/src/i18n/authErrors.ts` | **New** — 登录/注册错误码 → message key |
| `codes/web/src/i18n/authErrors.test.ts` | **New** — 错误映射 |
| `codes/web/messages/zh.json` | **New** — 简体中文（第一期全部 key） |
| `codes/web/messages/en.json` | **New** — 英文，key 与 zh 相同 |
| `codes/web/scripts/check-i18n-keys.mjs` | **New** — 两套 json key 集合一致 |
| `codes/web/next.config.js` | plugin 包一层，保留 rewrites / standalone |
| `codes/web/package.json` | 依赖 `next-intl`；脚本 `test:i18n` |
| `codes/web/app/layout.tsx` | Provider、`lang`、`generateMetadata` |
| `codes/web/src/components/layout/LocaleSwitcher.tsx` | **New** — `menu` / `footer` |
| `codes/web/src/components/layout/NavItem.tsx` | badge `aria-label` 走 i18n |
| `codes/web/src/components/layout/SideNav.tsx` | 导航 + 头像菜单 |
| `codes/web/src/components/layout/TopBar.tsx` | 退出登录 + 语言菜单 |
| `codes/web/src/components/layout/AppShell.tsx` | 浮动按钮 `aria-label` |
| `codes/web/src/components/layout/index.ts` | 导出 `LocaleSwitcher` |
| `codes/web/src/components/Breadcrumbs.tsx` | 固定段名与 aria |
| `codes/web/app/login/page.tsx` | 文案 + 页脚切换 |
| `codes/web/app/register/page.tsx` | 文案 + 页脚切换 |
| `.cursor/rules/ui-interaction-design.mdc` | 界面文案语言条款 |

`crumb.workoutDetail` 不在 spec 对照表里，但 `Breadcrumbs` 已有固定段「训练详情」，第一期一并进 json（壳层）。健康指标名仍用 `HEALTH_CARD_LABELS`（内容页，保持中文）。

---

### Task 1: Locale 纯函数、文案文件、key 一致性

**Files:**
- Create: `codes/web/src/i18n/config.ts`
- Create: `codes/web/src/i18n/config.test.ts`
- Create: `codes/web/messages/zh.json`
- Create: `codes/web/messages/en.json`
- Create: `codes/web/scripts/check-i18n-keys.mjs`
- Modify: `codes/web/package.json`（只加 `test:i18n` 脚本，本任务不装 `next-intl`）

**Interfaces:**
- Consumes: nothing
- Produces: `LOCALES`, `DEFAULT_LOCALE`, `LOCALE_COOKIE`, `Locale`, `isLocale`, `resolveLocale`, `htmlLang`; 完整 `messages/zh.json` 与 `messages/en.json`

- [x] **Step 1: Write the failing locale tests**

`codes/web/src/i18n/config.test.ts`:

```ts
import assert from "node:assert/strict";
import { describe, it } from "node:test";
import { DEFAULT_LOCALE, htmlLang, isLocale, resolveLocale } from "./config";

describe("resolveLocale", () => {
  it("returns zh when value is missing", () => {
    assert.equal(resolveLocale(undefined), "zh");
    assert.equal(resolveLocale(null), "zh");
    assert.equal(resolveLocale(""), "zh");
  });

  it("accepts zh and en", () => {
    assert.equal(resolveLocale("zh"), "zh");
    assert.equal(resolveLocale("en"), "en");
  });

  it("rejects zh-CN, en-US, and junk", () => {
    assert.equal(resolveLocale("zh-CN"), "zh");
    assert.equal(resolveLocale("en-US"), "zh");
    assert.equal(resolveLocale("fr"), "zh");
  });
});

describe("isLocale", () => {
  it("is a type guard for zh and en only", () => {
    assert.equal(isLocale("zh"), true);
    assert.equal(isLocale("en"), true);
    assert.equal(isLocale("zh-CN"), false);
  });
});

describe("htmlLang", () => {
  it("maps zh to zh-CN and en to en", () => {
    assert.equal(htmlLang("zh"), "zh-CN");
    assert.equal(htmlLang("en"), "en");
  });
});

describe("DEFAULT_LOCALE", () => {
  it("is zh", () => {
    assert.equal(DEFAULT_LOCALE, "zh");
  });
});
```

- [x] **Step 2: Run tests to verify they fail**

Run: `cd codes/web && node --experimental-strip-types --test src/i18n/config.test.ts`

Expected: FAIL（找不到 `./config` 或 `resolveLocale` 未定义）

若 Node 不支持 `--experimental-strip-types`，改用：`npx --yes tsx --test src/i18n/config.test.ts`

- [x] **Step 3: Implement config and both message files**

`codes/web/src/i18n/config.ts`:

```ts
export const LOCALES = ["zh", "en"] as const;
export type Locale = (typeof LOCALES)[number];
export const DEFAULT_LOCALE: Locale = "zh";
export const LOCALE_COOKIE = "locale";
export const LOCALE_MAX_AGE = 31536000;

export function isLocale(value: string | null | undefined): value is Locale {
  return value === "zh" || value === "en";
}

export function resolveLocale(value: string | null | undefined): Locale {
  return isLocale(value) ? value : DEFAULT_LOCALE;
}

export function htmlLang(locale: Locale): "zh-CN" | "en" {
  return locale === "zh" ? "zh-CN" : "en";
}
```

`codes/web/messages/zh.json`（完整，含 `crumb.workoutDetail`）：

```json
{
  "meta": { "title": "Timia · 协作管理" },
  "nav": {
    "schedule": "我的日程",
    "workspaces": "工作空间",
    "health": "健康",
    "plans": "规划",
    "analytics": "数据分析",
    "members": "成员",
    "codeDocs": "代码文档",
    "guide": "使用指南",
    "newTask": "新建任务",
    "openStickyNote": "打开便利贴",
    "logOut": "退出登录",
    "userFallback": "用户",
    "badgeAria": "{label}，{count} 条提醒"
  },
  "crumb": {
    "navAria": "面包屑导航",
    "refreshAria": "刷新 {label}",
    "workspaces": "工作空间",
    "projects": "项目",
    "items": "任务",
    "activity": "活动",
    "settings": "设置",
    "members": "成员",
    "documents": "文档",
    "code": "代码文档",
    "guide": "使用指南",
    "database": "数据库结构",
    "api": "后端 API",
    "my": "我的",
    "schedule": "日程",
    "analytics": "数据分析",
    "health": "健康",
    "plans": "规划",
    "new": "新建",
    "edit": "编辑",
    "period": "周期详情",
    "mySchedule": "我的日程",
    "workoutDetail": "训练详情"
  },
  "locale": {
    "zh": "中文",
    "en": "English",
    "switchAria": "界面语言"
  },
  "auth": {
    "login": "登录",
    "loggingIn": "登录中…",
    "register": "注册",
    "createAccount": "创建账号",
    "registering": "注册中…",
    "email": "邮箱",
    "password": "密码",
    "displayName": "显示名称",
    "confirmPassword": "确认密码",
    "tagline": "合抱之木，生于毫末；九层之台，起于累土；千里之行，始于足下",
    "noAccount": "还没有账号？",
    "hasAccount": "已有账号？",
    "goLogin": "去登录",
    "privacy": "隐私",
    "copyright": "Copyright © 2026 Timia",
    "emailPlaceholder": "Email",
    "passwordPlaceholder": "Password",
    "registerEmailPlaceholder": "name@company.com",
    "displayNamePlaceholder": "例如：王伟",
    "newPasswordPlaceholder": "至少 8 位字符",
    "confirmPasswordPlaceholder": "再次输入密码",
    "needEmailAndPassword": "请输入邮箱和密码",
    "needEmail": "请输入邮箱",
    "needPassword": "请输入密码",
    "needDisplayName": "请输入显示名",
    "invalidEmail": "请输入有效的邮箱地址",
    "passwordTooShort": "密码至少 8 位",
    "passwordMismatch": "两次输入的密码不一致",
    "sessionExpired": "登录已过期，请重新登录。",
    "loginFailed": "登录失败，请稍后重试",
    "invalidCredentials": "邮箱或密码错误，请重新输入",
    "invalidLoginFormat": "登录信息格式有误，请检查后重试",
    "serverUnavailable": "服务器暂时不可用，请稍后重试",
    "networkError": "无法连接服务器，请检查网络后重试",
    "registerFailed": "注册失败",
    "emailTaken": "该邮箱已被注册",
    "displayNameTaken": "该显示名称已被使用",
    "displayNameRequired": "请输入显示名称",
    "displayNameTooLong": "显示名称过长"
  }
}
```

`codes/web/messages/en.json`：

```json
{
  "meta": { "title": "Timia · Collaboration" },
  "nav": {
    "schedule": "My schedule",
    "workspaces": "Workspaces",
    "health": "Health",
    "plans": "Plans",
    "analytics": "Analytics",
    "members": "Members",
    "codeDocs": "Code docs",
    "guide": "Guide",
    "newTask": "New task",
    "openStickyNote": "Open sticky notes",
    "logOut": "Log out",
    "userFallback": "User",
    "badgeAria": "{label}, {count, plural, one {# alert} other {# alerts}}"
  },
  "crumb": {
    "navAria": "Breadcrumb",
    "refreshAria": "Refresh {label}",
    "workspaces": "Workspaces",
    "projects": "Projects",
    "items": "Tasks",
    "activity": "Activity",
    "settings": "Settings",
    "members": "Members",
    "documents": "Docs",
    "code": "Code docs",
    "guide": "Guide",
    "database": "Database",
    "api": "API",
    "my": "My",
    "schedule": "Schedule",
    "analytics": "Analytics",
    "health": "Health",
    "plans": "Plans",
    "new": "New",
    "edit": "Edit",
    "period": "Period",
    "mySchedule": "My schedule",
    "workoutDetail": "Workout"
  },
  "locale": {
    "zh": "中文",
    "en": "English",
    "switchAria": "Language"
  },
  "auth": {
    "login": "Log in",
    "loggingIn": "Signing in…",
    "register": "Sign up",
    "createAccount": "Create account",
    "registering": "Creating account…",
    "email": "Email",
    "password": "Password",
    "displayName": "Display name",
    "confirmPassword": "Confirm password",
    "tagline": "A huge tree grows from a tiny shoot; a nine-story tower rises from a heap of earth; a journey of a thousand miles begins with a single step",
    "noAccount": "No account yet?",
    "hasAccount": "Already have an account?",
    "goLogin": "Log in",
    "privacy": "Privacy",
    "copyright": "Copyright © 2026 Timia",
    "emailPlaceholder": "Email",
    "passwordPlaceholder": "Password",
    "registerEmailPlaceholder": "name@company.com",
    "displayNamePlaceholder": "e.g. Alex",
    "newPasswordPlaceholder": "At least 8 characters",
    "confirmPasswordPlaceholder": "Enter password again",
    "needEmailAndPassword": "Enter email and password",
    "needPassword": "Enter your password",
    "needEmail": "Enter your email",
    "needDisplayName": "Enter a display name",
    "invalidEmail": "Enter a valid email address",
    "passwordTooShort": "Password must be at least 8 characters",
    "passwordMismatch": "Passwords do not match",
    "sessionExpired": "Your session expired. Please log in again.",
    "loginFailed": "Sign-in failed. Try again later.",
    "invalidCredentials": "Incorrect email or password",
    "invalidLoginFormat": "Check the form and try again",
    "serverUnavailable": "Server is temporarily unavailable",
    "networkError": "Cannot reach the server. Check your network",
    "registerFailed": "Could not create account",
    "emailTaken": "That email is already registered",
    "displayNameTaken": "That display name is already taken",
    "displayNameRequired": "Enter a display name",
    "displayNameTooLong": "Display name is too long"
  }
}
```

`codes/web/scripts/check-i18n-keys.mjs`:

```js
import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const root = path.join(path.dirname(fileURLToPath(import.meta.url)), "..");

function flatten(value, prefix = "") {
  if (value && typeof value === "object" && !Array.isArray(value)) {
    return Object.entries(value).flatMap(([key, child]) =>
      flatten(child, prefix ? `${prefix}.${key}` : key),
    );
  }
  return [prefix];
}

const zh = JSON.parse(fs.readFileSync(path.join(root, "messages/zh.json"), "utf8"));
const en = JSON.parse(fs.readFileSync(path.join(root, "messages/en.json"), "utf8"));
const zhKeys = new Set(flatten(zh));
const enKeys = new Set(flatten(en));
const missingEn = [...zhKeys].filter((key) => !enKeys.has(key));
const missingZh = [...enKeys].filter((key) => !zhKeys.has(key));
if (missingEn.length || missingZh.length) {
  console.error("zh extra vs en:", missingEn);
  console.error("en extra vs zh:", missingZh);
  process.exit(1);
}
if (zh.locale.zh !== "中文" || en.locale.zh !== "中文" || zh.locale.en !== "English" || en.locale.en !== "English") {
  console.error("locale.zh / locale.en must be identical in both files");
  process.exit(1);
}
console.log(`i18n keys ok (${zhKeys.size})`);
```

在 `codes/web/package.json` 的 `scripts` 中增加：

```json
"test:i18n": "node --experimental-strip-types --test src/i18n/*.test.ts && node scripts/check-i18n-keys.mjs"
```

（若 strip-types 不可用，把前半改成 `npx --yes tsx --test src/i18n/*.test.ts`。）

- [x] **Step 4: Run tests to verify they pass**

Run: `cd codes/web && npm run test:i18n`

Expected: `resolveLocale` 测试 PASS；打印 `i18n keys ok (N)`，exit 0

---

### Task 2: 安装 next-intl 并接到根 layout

**Files:**
- Create: `codes/web/src/i18n/routing.ts`
- Create: `codes/web/src/i18n/request.ts`
- Create: `codes/web/src/i18n/actions.ts`
- Create: `codes/web/src/i18n/next-intl.d.ts`
- Modify: `codes/web/next.config.js`
- Modify: `codes/web/package.json`（依赖）
- Modify: `codes/web/app/layout.tsx`

**Interfaces:**
- Consumes: `resolveLocale`, `htmlLang`, `LOCALE_COOKIE`, `LOCALE_MAX_AGE`, `isLocale`, `Locale` from Task 1
- Produces: `changeLocaleAction(locale: Locale) => Promise<void>`；根树可 `useTranslations` / `useLocale`

- [x] **Step 1: Install next-intl**

Run: `cd codes/web && npm install next-intl`

Expected: `package.json` / lockfile 出现 `next-intl`，命令 exit 0

- [x] **Step 2: Wire plugin, request config, types, and locale action**

`codes/web/src/i18n/routing.ts`:

```ts
import { defineRouting } from "next-intl/routing";
import { DEFAULT_LOCALE, LOCALES } from "./config";

export const routing = defineRouting({
  locales: LOCALES,
  defaultLocale: DEFAULT_LOCALE,
  localePrefix: "never",
});
```

`codes/web/src/i18n/request.ts`:

```ts
import { cookies } from "next/headers";
import { getRequestConfig } from "next-intl/server";
import { LOCALE_COOKIE, resolveLocale } from "./config";

export default getRequestConfig(async () => {
  const store = await cookies();
  const locale = resolveLocale(store.get(LOCALE_COOKIE)?.value);
  return {
    locale,
    messages: (await import(`../../messages/${locale}.json`)).default,
  };
});
```

`codes/web/src/i18n/actions.ts`:

```ts
"use server";

import { cookies } from "next/headers";
import { LOCALE_COOKIE, LOCALE_MAX_AGE, isLocale, type Locale } from "./config";

export async function changeLocaleAction(locale: Locale): Promise<void> {
  if (!isLocale(locale)) return;
  const store = await cookies();
  store.set(LOCALE_COOKIE, locale, {
    path: "/",
    maxAge: LOCALE_MAX_AGE,
    sameSite: "lax",
    secure: process.env.NODE_ENV === "production",
    httpOnly: false,
  });
}
```

`codes/web/src/i18n/next-intl.d.ts`:

```ts
import type { Locale } from "./config";
import type zh from "../../messages/zh.json";

declare module "next-intl" {
  interface AppConfig {
    Locale: Locale;
    Messages: typeof zh;
  }
}
```

`codes/web/next.config.js` 全文件替换为（rewrites 与现有一致）：

```js
const createNextIntlPlugin = require("next-intl/plugin");

const withNextIntl = createNextIntlPlugin("./src/i18n/request.ts");

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "standalone",
  allowedDevOrigins: ["127.0.0.1", "localhost"],
  async rewrites() {
    return [
      {
        source: "/auth/:path*",
        destination: "http://localhost:8000/auth/:path*",
      },
      {
        source: "/sticky-notes/:path*",
        destination: "http://localhost:8000/sticky-notes/:path*",
      },
      {
        source: "/health/:path*",
        destination: "http://localhost:8000/health/:path*",
      },
    ];
  },
};

module.exports = withNextIntl(nextConfig);
```

- [x] **Step 3: Update root layout**

`codes/web/app/layout.tsx` 全文件：

```tsx
import "./globals.css";
import type { Metadata } from "next";
import type { ReactNode } from "react";
import { Be_Vietnam_Pro, Epilogue, JetBrains_Mono } from "next/font/google";
import { NextIntlClientProvider } from "next-intl";
import { getLocale, getMessages, getTranslations } from "next-intl/server";
import { htmlLang, type Locale } from "@/i18n/config";

const epilogue = Epilogue({
  subsets: ["latin"],
  variable: "--font-epilogue",
  weight: ["400", "500", "600", "700", "800"],
});

const beVietnamPro = Be_Vietnam_Pro({
  subsets: ["latin"],
  variable: "--font-be-vietnam",
  weight: ["400", "500", "600", "700"],
});

const jetBrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-jetbrains-mono",
  weight: ["400", "500", "600", "700"],
});

export async function generateMetadata(): Promise<Metadata> {
  const t = await getTranslations("meta");
  return {
    title: t("title"),
    icons: {
      icon: [{ url: "/icon.png?v=20260724-t", sizes: "512x512", type: "image/png" }],
      shortcut: [{ url: "/favicon.ico?v=20260724-t", type: "image/x-icon" }],
      apple: [{ url: "/icon.png?v=20260724-t", sizes: "512x512", type: "image/png" }],
    },
  };
}

export default async function RootLayout({ children }: { children: ReactNode }) {
  const locale = (await getLocale()) as Locale;
  const messages = await getMessages();
  return (
    <html lang={htmlLang(locale)}>
      <head>
        <link
          rel="stylesheet"
          href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
        />
      </head>
      <body
        className={[
          epilogue.variable,
          beVietnamPro.variable,
          jetBrainsMono.variable,
          "bg-background font-body text-on-background selection:bg-primary-fixed selection:text-on-primary-fixed",
        ].join(" ")}
      >
        <NextIntlClientProvider messages={messages}>{children}</NextIntlClientProvider>
      </body>
    </html>
  );
}
```

不要加 `app/[locale]`，不要加 `middleware.ts`。

- [x] **Step 4: Confirm the app still boots**

Run: `cd codes/web && npx tsc --noEmit`

Expected: exit 0（若 `next-intl` 的 `AppConfig` 模块增强报错，把 `next-intl.d.ts` 的 `declare module` 改成文档当前版本要求的名字，不要改 cookie 语义）

手动打开 `http://localhost:3000/login`：无 cookie 时标题仍是「Timia · 协作管理」，`document.documentElement.lang === "zh-CN"`。

---

### Task 3: LocaleSwitcher

**Files:**
- Create: `codes/web/src/components/layout/LocaleSwitcher.tsx`
- Modify: `codes/web/src/components/layout/index.ts`

**Interfaces:**
- Consumes: `changeLocaleAction` from Task 2；`Locale` from Task 1；messages `locale.*`
- Produces: `LocaleSwitcher({ variant: "menu" | "footer" })`

- [x] **Step 1: Implement LocaleSwitcher**

`codes/web/src/components/layout/LocaleSwitcher.tsx`:

```tsx
"use client";

import { useLocale, useTranslations } from "next-intl";
import { useRouter } from "next/navigation";
import { changeLocaleAction } from "@/i18n/actions";
import { LOCALES, type Locale } from "@/i18n/config";

type LocaleSwitcherProps = {
  variant: "menu" | "footer";
};

export function LocaleSwitcher({ variant }: LocaleSwitcherProps) {
  const locale = useLocale() as Locale;
  const t = useTranslations("locale");
  const router = useRouter();

  async function select(next: Locale) {
    if (next === locale) return;
    await changeLocaleAction(next);
    router.refresh();
  }

  if (variant === "footer") {
    return (
      <div className="flex items-center gap-sm" aria-label={t("switchAria")}>
        {LOCALES.map((code, index) => (
          <span key={code} className="inline-flex items-center gap-sm">
            {index > 0 ? <span aria-hidden>·</span> : null}
            <button
              type="button"
              className={
                code === locale
                  ? "text-text-primary"
                  : "transition-colors hover:text-text-secondary"
              }
              aria-current={code === locale ? "true" : undefined}
              onClick={() => void select(code)}
            >
              {t(code)}
            </button>
          </span>
        ))}
      </div>
    );
  }

  return (
    <div role="group" aria-label={t("switchAria")}>
      {LOCALES.map((code) => (
        <button
          key={code}
          type="button"
          role="menuitemradio"
          aria-checked={code === locale}
          className="flex w-full items-center justify-between px-3 py-2 text-left text-small text-text-secondary transition-colors hover:bg-surface-container-lowest"
          onClick={() => void select(code)}
        >
          <span>{t(code)}</span>
          {code === locale ? <span aria-hidden>✓</span> : null}
        </button>
      ))}
    </div>
  );
}
```

`codes/web/src/components/layout/index.ts` 增加：

```ts
export { LocaleSwitcher } from "./LocaleSwitcher";
```

- [x] **Step 2: Typecheck**

Run: `cd codes/web && npx tsc --noEmit`

Expected: exit 0

本任务不接入页面；下一任务把开关放进菜单和页脚。

---

### Task 4: 壳层（导航、头像菜单、浮动按钮）

**Files:**
- Modify: `codes/web/src/components/layout/NavItem.tsx`
- Modify: `codes/web/src/components/layout/SideNav.tsx`
- Modify: `codes/web/src/components/layout/TopBar.tsx`
- Modify: `codes/web/src/components/layout/AppShell.tsx`

**Interfaces:**
- Consumes: `useTranslations("nav")`；`LocaleSwitcher` variant `menu`
- Produces: 登录后壳层文案随 locale 变化

- [x] **Step 1: NavItem badge aria**

`NavItem.tsx`：增加 `useTranslations`。`label` 仍由父组件传入（已翻译）。有 badge 时：

```tsx
"use client";

import Link from "next/link";
import { useTranslations } from "next-intl";

export type NavItemProps = {
  href: string;
  icon: string;
  label: string;
  active: boolean;
  hidden?: boolean;
  badge?: number;
};

export function NavItem({ href, icon, label, active, hidden, badge }: NavItemProps) {
  const t = useTranslations("nav");
  const showBadge = typeof badge === "number" && badge > 0;
  const badgeText = showBadge && badge > 99 ? "99+" : String(badge ?? "");
  const ariaLabel = showBadge ? t("badgeAria", { label, count: badge ?? 0 }) : label;
  // ...其余 JSX 与现文件相同，仅 aria-label={ariaLabel}
}
```

把文件顶部改成 `"use client"`（已是 client 也可保留）。现有 className / tooltip 结构不要改。

- [x] **Step 2: SideNav labels and user menu**

在 `SideNav` 内：

```tsx
const t = useTranslations("nav");
const displayName = me?.display_name?.trim() || t("userFallback");
```

各 `NavItem` 的 `label`：

- `t("schedule")` / `t("workspaces")` / `t("health")` / `t("plans")` / `t("analytics")` / `t("members")` / `t("codeDocs")` / `t("guide")`

头像菜单容器 `w-32` 改为 `w-40`。在「退出登录」按钮**上方**插入 `<LocaleSwitcher variant="menu" />`。退出按钮文案改为 `t("logOut")`。

- [x] **Step 3: TopBar user menu**

同样：`useTranslations("nav")`，菜单 `w-40`，先 `<LocaleSwitcher variant="menu" />`，再退出按钮 `t("logOut")`。

- [x] **Step 4: AppShell floating buttons**

`AppShell` 是 client 组件。在渲染浮动按钮的组件内：

```tsx
const t = useTranslations("nav");
```

`ariaLabel="新建任务"` → `ariaLabel={t("newTask")}`  
`ariaLabel="打开便利贴"` → `ariaLabel={t("openStickyNote")}`

- [x] **Step 5: Typecheck**

Run: `cd codes/web && npx tsc --noEmit`

Expected: exit 0

---

### Task 5: Breadcrumbs 固定段

**Files:**
- Modify: `codes/web/src/components/Breadcrumbs.tsx`

**Interfaces:**
- Consumes: `useTranslations("crumb")`
- Produces: 固定段与 `aria-label` 随语言变化；工作空间/项目/规划名称仍来自缓存

- [x] **Step 1: Replace hardcoded Chinese segment labels**

在组件函数内（已是 `"use client"`）：

```tsx
const t = useTranslations("crumb");
const locale = useLocale();
```

`defaultLabelBySegment` 改为：

```ts
const defaultLabelBySegment: Record<string, string> = {
  workspaces: t("workspaces"),
  workspace: t("workspaces"),
  projects: t("projects"),
  project: t("projects"),
  items: t("items"),
  item: t("items"),
  activity: t("activity"),
  settings: t("settings"),
  members: t("members"),
  documents: t("documents"),
  code: t("code"),
  guide: t("guide"),
  database: t("database"),
  api: t("api"),
  my: t("my"),
  schedule: t("schedule"),
  analytics: t("analytics"),
  health: t("health"),
  plans: t("plans"),
  new: t("new"),
  edit: t("edit"),
  period: t("period"),
};
```

合并 crumb：

- `/my/schedule` → `t("mySchedule")`（替换字面量 `"我的日程"`）
- `/my/analytics` → `t("analytics")`
- `/my/health` → `t("health")`
- workouts 详情 → `t("workoutDetail")`（替换 `"训练详情"`）

id 占位：`"项目"` → `t("projects")`，`"任务"` → `t("items")`

`<nav aria-label="面包屑导航">` → `aria-label={t("navAria")}`  
`aria-label={\`刷新 ${c.label}\`}` → `aria-label={t("refreshAria", { label: c.label })}`

`useMemo` 依赖数组加上 `locale`（不要依赖不稳定的 `t` 引用若 eslint 要求则两者都加）。

健康指标段继续 `HEALTH_CARD_LABELS[segment]`，第一期不译。

- [x] **Step 2: Typecheck**

Run: `cd codes/web && npx tsc --noEmit`

Expected: exit 0

---

### Task 6: 登录、注册、错误码映射

**Files:**
- Create: `codes/web/src/i18n/authErrors.ts`
- Create: `codes/web/src/i18n/authErrors.test.ts`
- Modify: `codes/web/app/login/page.tsx`
- Modify: `codes/web/app/register/page.tsx`

**Interfaces:**
- Consumes: `ApiError`；`useTranslations("auth")`；`LocaleSwitcher` variant `footer`
- Produces: `loginErrorKey(error: unknown) => LoginErrorKey`；`registerErrorKey(detail: string) => RegisterErrorKey`

- [x] **Step 1: Write failing auth error tests**

`codes/web/src/i18n/authErrors.ts` 先写类型与空实现也可；测试文件：

```ts
import assert from "node:assert/strict";
import { describe, it } from "node:test";
import { loginErrorKey, registerErrorKey } from "./authErrors";

describe("loginErrorKey", () => {
  it("maps 401 and invalid_credentials", () => {
    assert.equal(loginErrorKey({ status: 401, message: "nope" }), "invalidCredentials");
    assert.equal(loginErrorKey({ status: 400, message: "invalid_credentials" }), "invalidCredentials");
  });

  it("maps 422 and 5xx", () => {
    assert.equal(loginErrorKey({ status: 422, message: "x" }), "invalidLoginFormat");
    assert.equal(loginErrorKey({ status: 503, message: "x" }), "serverUnavailable");
  });

  it("does not pass through Chinese detail", () => {
    assert.equal(loginErrorKey({ status: 400, message: "邮箱不对" }), "loginFailed");
  });

  it("falls back for empty/unknown", () => {
    assert.equal(loginErrorKey(null), "loginFailed");
    assert.equal(loginErrorKey({ status: 400, message: "weird_code" }), "loginFailed");
  });

  it("maps fetch TypeError to networkError", () => {
    assert.equal(loginErrorKey(new TypeError("Failed to fetch")), "networkError");
    assert.equal(loginErrorKey({ status: 0, message: "Failed to fetch" }), "networkError");
  });
});

describe("registerErrorKey", () => {
  it("maps known codes", () => {
    assert.equal(registerErrorKey("email_taken"), "emailTaken");
    assert.equal(registerErrorKey("display_name_taken"), "displayNameTaken");
    assert.equal(registerErrorKey("password_too_short"), "passwordTooShort");
    assert.equal(registerErrorKey("display_name_required"), "displayNameRequired");
    assert.equal(registerErrorKey("display_name_too_long"), "displayNameTooLong");
  });

  it("unknown codes go to registerFailed", () => {
    assert.equal(registerErrorKey("nope"), "registerFailed");
    assert.equal(registerErrorKey("该邮箱已被注册"), "registerFailed");
  });
});
```

- [x] **Step 2: Run tests to verify they fail**

Run: `cd codes/web && node --experimental-strip-types --test src/i18n/authErrors.test.ts`

Expected: FAIL（模块不存在）

- [x] **Step 3: Implement authErrors.ts**

```ts
type LoginErrorKey =
  | "loginFailed"
  | "invalidCredentials"
  | "invalidLoginFormat"
  | "serverUnavailable"
  | "networkError";

type RegisterErrorKey =
  | "registerFailed"
  | "emailTaken"
  | "displayNameTaken"
  | "passwordTooShort"
  | "displayNameRequired"
  | "displayNameTooLong";

export function loginErrorKey(error: unknown): LoginErrorKey {
  if (error instanceof TypeError) return "networkError";
  if (!error || typeof error !== "object") return "loginFailed";
  const apiError = error as { status?: number; message?: string };
  const message = typeof apiError.message === "string" ? apiError.message : "";
  if (message === "Failed to fetch") return "networkError";
  if (apiError.status === 401 || message === "invalid_credentials") return "invalidCredentials";
  if (apiError.status === 422) return "invalidLoginFormat";
  if (typeof apiError.status === "number" && apiError.status >= 500) return "serverUnavailable";
  return "loginFailed";
}

export function registerErrorKey(detail: string): RegisterErrorKey {
  switch (detail) {
    case "email_taken":
      return "emailTaken";
    case "display_name_taken":
      return "displayNameTaken";
    case "password_too_short":
      return "passwordTooShort";
    case "display_name_required":
      return "displayNameRequired";
    case "display_name_too_long":
      return "displayNameTooLong";
    default:
      return "registerFailed";
  }
}
```

不要根据汉字正则返回 `message` 原文。未知业务码用 `loginFailed`；网络失败用 `networkError`。

- [x] **Step 4: Run auth tests**

Run: `cd codes/web && npm run test:i18n`

Expected: 全部 PASS，含 `authErrors.test.ts`

- [x] **Step 5: Wire login page**

`login/page.tsx`：

- `const t = useTranslations("auth");`
- 删除 `loginErrorMessage`；`setError(t(loginErrorKey(err)))`
- 校验：`t("needEmailAndPassword")` 等
- `sessionExpired`：`t("sessionExpired")`
- 标签：`t("email")` / `t("password")`（中文界面不再写死 Email）
- placeholder 仍用 `t("emailPlaceholder")` / `t("passwordPlaceholder")`（CSS 浮动标签需要 placeholder）
- 按钮：`loading ? t("loggingIn") : t("login")`
- 底部：`t("noAccount")` + Link `t("register")`
- 页脚：`t("copyright")`，中间插 `<LocaleSwitcher variant="footer" />`，然后 `t("privacy")`
- tagline：`t("tagline")`；英文可能换行，保持 `text-center`，不要改 `max-w-[440px]`

- [x] **Step 6: Wire register page**

同样 `useTranslations("auth")`。校验与 `registerErrorKey`：

```ts
setError(t(registerErrorKey(String((err as { message?: string })?.message ?? "registerFailed"))));
```

未知码已经是 `registerFailed`。标签、placeholder、按钮、`t("hasAccount")` + `t("goLogin")`、页脚与登录页同一套 LocaleSwitcher。

- [x] **Step 7: Typecheck**

Run: `cd codes/web && npx tsc --noEmit`

Expected: exit 0

---

### Task 7: 规范条款与手工验收

**Files:**
- Modify: `.cursor/rules/ui-interaction-design.mdc`

**Interfaces:**
- Consumes: 第一期行为已就绪
- Produces: 规则与 spec §7 一致

- [x] **Step 1: Replace the UI copy language section**

把「界面文案语言」整段换成：

```markdown
## 界面文案语言

用户可见文案（标题、按钮、导航、表单、空状态、错误、对话框、`aria-label`、`title`、`alt`）走 `next-intl` 的 `useTranslations` / `messages/*.json`，不要在新代码里写死某一种语言。默认语言是简体中文（cookie `locale` 缺失时为 `zh`）。产品名 “Timia” 不译。API 枚举在界面上用 i18n key（后续模块建 `enums.*`）；未迁模块可暂时保持硬编码中文。

`html lang` 与日期/数字格式跟当前 locale（`zh` → `zh-CN`，`en` → `en-US`），不要写死 `zh-CN`。
```

「可访问性」里「根布局已为 `zh-CN`」改为「`html lang` 跟当前 locale」。

检查清单第 4 条「明确**简体中文**文案」改为「明确的 i18n 文案（默认中文）」。

- [x] **Step 2: Manual QA against spec §8**

用无痕窗口（无 `locale` cookie）：

1. 打开 `/login`：中文；`document.documentElement.lang === "zh-CN"`；标题「Timia · 协作管理」。
2. 页脚点 English：本页英文，URL 仍是 `/login`，cookie `locale=en`；刷新仍英文。
3. 点 中文：回到中文，URL 无前缀。
4. 点当前语言：不闪 refresh（Network 里不应出现整页重新导航到 `/my/schedule`）。
5. 英文登录后：侧栏 hover、面包屑固定段、退出登录、浮动按钮 aria 为英文；`/my/schedule` 正文可以仍是中文。
6. 窄屏顶栏头像菜单可切换语言。
7. `/register` 校验与模拟 `email_taken` 随语言变。
8. `cd codes/web && npm run test:i18n` 通过。

---

## Spec coverage

| Spec | Task |
|------|------|
| cookie / 默认 zh / 不读 Accept-Language | 1, 2 |
| 无 URL 前缀、无 `[locale]`、无 middleware | 2 |
| `html lang` + document title | 2 |
| next-intl plugin + Provider + server action | 2, 3 |
| LocaleSwitcher 登录页脚 + 头像菜单 | 3, 4, 6 |
| 壳层导航 / badge / 退出 / 浮动按钮 | 4 |
| Breadcrumbs | 5 |
| `/login` `/register` 文案与错误码 | 6 |
| 禁止汉字 detail 原样上屏 | 6 |
| zh/en key 一致 | 1, 7 |
| 规范条款 | 7 |
| 验收 1–8 | 7 |
| `crumb.workoutDetail`（壳层已有固定段） | 1, 5 |
| 后续模块 / users.locale / iOS | 不做 |

## 实现时不要做

- 不要改 `app/` 目录结构
- 不要翻译日程、健康等内容页
- 不要 `npm install` i18next / next-i18next
- 不要给 cookie 设 `HttpOnly: true`（spec 要求客户端可读写）
- 不要根据 `Accept-Language` 第一次访问改成英文
