# iOS 日程页面 Design QA

## 验收对象

- 运行环境：iPhone 17 模拟器，iOS 26.4.1，竖屏
- 逻辑视口：402 × 874 pt
- 实现截图：1206 × 2622 px（@3x）
- 状态：DEBUG 本地登录，2026 年 7 月，日历与 Todo 模式

## 对照材料

| 模式 | 参考图 | 实现截图 | 并排对照 |
| --- | --- | --- | --- |
| 日 | `docs/design-references/assets/calendar-day-layout-reference.png` | `docs/design-references/assets/calendar-day-layout-implementation.png` | `docs/design-references/assets/calendar-day-layout-comparison.png` |
| 周 | 用户问题截图 `calendar-week-spacing-before.png` | `docs/design-references/assets/calendar-week-layout-implementation.png` | `docs/design-references/assets/calendar-week-spacing-comparison.png` |
| 月 | `docs/design-references/assets/calendar-month-layout-reference.png` | `docs/design-references/assets/calendar-month-layout-implementation.png` | `docs/design-references/assets/calendar-month-layout-comparison.png` |
| 年 | `docs/design-references/assets/calendar-year-layout-reference.png` | `docs/design-references/assets/calendar-year-layout-implementation.png` | `docs/design-references/assets/calendar-year-layout-comparison.png` |
| Todo | 本轮用户布局说明 | `docs/design-references/assets/calendar-todo-layout-implementation.png` | 按文字规格逐项检查 |

## 检查结果

### 全局结构

- 通过：顶部左侧使用 `yyyy年MM月`，右侧依次为空间入口和可点击头像。
- 通过：底部模式切换、D/W/M/Y 视图切换和自然语言输入框层级清晰。
- 通过：未展示麦克风入口，输入框只支持手动文字提交。
- 通过：移除应用主 Tab Bar，内容和底部输入区获得更多可用高度。

### 日模式

- 通过：日期条固定在时间轴上方，7 个日期完整展示。
- 通过：时间刻度、虚线网格、任务卡和当前时间红线保持同一坐标系。
- 通过：首次进入当天自动定位到当前时间附近，同时仍可上下浏览全天。
- 通过：横向滑动切日、纵向滚动时间轴互不冲突。
- 通过：日期卡高度和上下留白已压缩，时间轴首屏密度更接近参考图。
- 通过：任务背景色、文字色和左侧强调线由 P1—P4 优先级统一驱动。

### 周模式

- 通过：同屏展示 7 天；日期头与时间列固定，任务内容按时间轴滚动。
- 通过：全天任务行与当前周内容同步，左右滑动切换整周。
- 通过：周头严格固定为 42 pt；左侧时间轴占位不再纵向拉伸，日期上下的大面积空白已消除。
- 通过：日期头之后直接衔接全天行或时间网格，周任务使用同一优先级色板。

### 月模式

- 已调整：月份改为连续纵向滚动；每月仅展示真实日期，月初保留星期对齐空位，月末不再补下个月日期。
- 通过：任务摘要保持紧凑，超出容量显示 `+N`，非本月日期弱化。
- 通过：月任务背景使用优先级色板，不再依赖任务自定义背景色。
- 说明：截图中的任务名称与参考图不同，属于真实测试数据差异，不是布局缺陷。

### 年模式

- 通过：12 个月按 3 × 4 网格展示，选中月份有边框。
- 通过：每日任务量以热度色块表达，结构和密度与参考图一致。

### 交互与反馈

- 通过：头像跳转“我的”页面。
- 通过：空间入口位于头像左侧，可进入空间页并通过原生返回按钮返回日程。
- 通过：Todo/日历模式可切换；Todo 时 D/W/M/Y 完全隐藏，返回日历时选择器从左侧滑出。
- 通过：自然语言解析后进入可编辑确认页，再创建任务。
- 通过：错误使用屏幕中央临时 Tip，自动消失，不占据顶部布局。

### Todo 模式

- 通过：任务卡按 P1 蓝、P2 绿、P3 黄、P4 红展示语义背景色。
- 通过：左侧标题下方显示开始到结束时间；无时间任务明确显示“未设置时间”。
- 通过：右侧分两行显示空间与项目名称，长名称单行截断，不挤压主标题。
- 通过：已完成和已归档首次各返回最近 5 条，按钮展示剩余数量。
- 通过：每次展开独立拉取 10 条，加载中禁用重复点击，并根据 `has_more` 决定是否继续展示。

## 必检表面

- 字体：使用 SF 系统语义字号；标题、时间、空间/项目形成清晰三级层级，长标题最多两行。
- 间距：顶部和日期条留白已压缩；Todo 卡片保持 12–14 pt 内边距与稳定分区节奏。
- 色彩：优先级色板与 Web 端一致，并提供深色模式对应色；文字对比度满足可读性。
- 图像：该页面没有业务图片；头像为真实用户首字母，图标均使用 SF Symbols。
- 文案：模式、空间入口、分页、空状态和未设置时间文案均与当前中文产品语境一致。

## 比对迭代

1. 第一轮截图发现日模式首次进入停留在 00:00。
2. 将滚动锚点从视觉 `offset` 元素改为参与布局的逐小时锚点。
3. 第二轮截图确认页面自动定位到 11:00—16:00 区间，当前时间线可见。
4. 本轮第一张 Todo 截图发现从年视图切入后标题仍为单独年份；已改为 Todo 始终显示年月。
5. 本轮交互检查发现空间页沿用原 Tab 根页面时隐藏导航栏；已恢复“空间”导航栏和原生返回按钮。
6. 用户问题截图发现周日期头左侧 `Color.clear` 只限制宽度，作为弹性视图在 `HStack` 内纵向撑满可用空间。
7. 为占位视图和整个周日期头增加 42 pt 固定高度，阻断纵向扩张。
8. 修复后截图确认日期行紧贴顶部工具区，随后直接进入全天行或时间网格；原有大面积空白消失。

## 缺陷分级

- P0：0
- P1：0
- P2：0
- P3：0

## 自动化证据

- `TimiaUITests/testScheduleRedesignModes`：通过
- 验证内容：日/周/月/年/Todo 截图、周日期头高度、空间进入与返回、头像入口、无 Tab Bar、选择器显示/隐藏、无麦克风图标
- 结果包：`/tmp/timia-schedule-week-spacing-20260730-1340.xcresult`

final result: passed

## 月模式仅高亮当天（2026-07-31）

- 通过：月模式日期高亮不再依赖用于滚动和加载的 `selectedDate`。
- 通过：上下滚动月份时仍以每月 1 日作为数据锚点，但不会将该日期渲染为选中状态。
- 通过：仅系统当天显示深色圆形高亮；当前月份之外不显示任何日期高亮。
- 自动化验证：2026-07-31 显示“今天”，2026-07-01 和滚动后的 2026-08-01 均保持普通状态。
- 结果包：`/tmp/timia-month-today-only-20260731-1300.xcresult`

final result: passed

## 周模式连续拼接与自定义周序号（2026-07-30）

- 通过：顶部 7 天日期头改为纯展示，不再响应点击；仅当日期是真实当天时显示深色高亮。
- 通过：周视图改为纵向惰性周序列，每周完整时间轴首尾连续拼接，不再使用手势结束后整周替换。
- 通过：当前周首次进入采用两阶段定位，先生成当天所在周，再滚动到当前时间附近，避免惰性列表落到最早周。
- 通过：滚动进入前后周时，固定日期头、顶部年月和后台周数据同步更新。
- 通过：每周 00:00 上方显示小字 `yyyy年第N周`；周序号以本年度第一个完整的周日—周六自然周为第 1 周。
- 示例：2026 年第一个完整周从 1 月 4 日开始；2026 年 8 月 2 日所在周显示为 `2026年第31周`。
- 通过：继续沿用周模式当前周前后各 3 周并发预取与 7 份范围缓存策略。

### 自动化证据

- `TimiaUITests/testCalendarBlankCreateAndVerticalRangeNavigation`：通过
- `TimiaUITests/testScheduleRedesignModes`：通过
- 验证内容：周头日期不可点击、2026-07-30 显示“今天”、周空白时间新建、连续滚动进入 2026-08-02 周、顶部年月同步、`2026年第31周` 周序号、四视图与 Todo 回归
- 交互结果包：`/tmp/timia-week-continuous-two-stage-20260730-2040.xcresult`
- 模式结果包：`/tmp/timia-week-continuous-modes-20260730-2100.xcresult`

final result: passed

## 月模式连续月份滚动（2026-07-30）

- 已实现：月模式由单月切换手势改为原生纵向惯性滚动，上月末尾与下月标题、日期自然衔接。
- 已实现：每个月区段顶部显示 `yyyy年MM月`，月份区段到达顶部后同步替换固定头部月份。
- 已实现：固定头部旧月份原位淡出，新月份从下方滑入并淡入；减弱动态效果模式下降级为淡入淡出。
- 已实现：月初只保留与星期列对齐的空白格；当月最后一天之后立即结束网格，不显示下个月日期。
- 已实现：可见月份变化后优先使用预取缓存更新任务，随后后台刷新当前及相邻月份数据。
- 已补充：UI 自动化检查月份区段标题、固定头部月份值、跨月同步及空白日期区域新建任务。
- 验证状态：Swift 文件语法检查、Xcode 编译和模拟器回归均已通过；月模式可在点击后及时完成首屏展示。

final result: passed

## 年份区段标题与顶部替换转场（2026-07-30）

- 已实现：每个年份区段在 1 月上方显示 `yyyy年`，随月份内容连续滚动。
- 已实现：通过年份区段相对滚动容器顶部的位置判断跨年，仅在新年份标题到达顶部时替换固定头部年份。
- 已实现：旧年份采用原位淡出，新年份从下方滑入并淡入，转场时长 0.3 秒。
- 已实现：开启“减弱动态效果”时自动降级为短时淡入淡出。
- 已补充：UI 自动化分别校验区段年份标题、固定头部年份值及跨年后的同步结果。
- 验证状态：年份标题与转场布局的 SwiftUI 目标编译和模拟器回归均已通过；初始布局偏移不会再误触发年份替换。

final result: passed

## 年模式连续月份滚动（2026-07-30）

- 通过：移除年模式手势结束后的整年替换动画，改为原生纵向惯性滚动。
- 通过：每年 12 个月保持 3 × 4 网格，12 月下方直接衔接下一年 1 月，反向滚动同样连续。
- 通过：滚动跨越年份边界时顶部年份自动同步，不重置滚动位置、不闪屏。
- 通过：进入相邻年份时优先展示已经预取的年份数据，同时继续后台加载新的前后年份。
- 通过：月份卡片保持稳定标识，缓存数据返回时只更新热度内容，不改变滚动结构。

### 自动化证据

- `TimiaUITests/testCalendarBlankCreateAndVerticalRangeNavigation`：通过
- `TimiaUITests/testScheduleRedesignModes`：通过
- 验证内容：2026 年 12 月可见、连续滚动进入 2027 年区段、顶部年份同步
- 结果包：`/tmp/timia-year-continuous-final-20260730-1600.xcresult`
- 模式回归结果包：`/tmp/timia-year-continuous-modes-20260730-1605.xcresult`

final result: passed

## 日历跨度与空白区域交互（2026-07-30）

### 新建任务

- 通过：日模式点击空白时间格，按所点日期和 15 分钟粒度时间打开“新建任务”浮层。
- 通过：周模式点击任意一天的空白时间格，按对应日期和时间打开“新建任务”浮层。
- 通过：月模式点击日期单元格空白处，按全天日期打开“新建任务”浮层；日期与已有任务按钮仍保持各自交互。

### 日期跨度

- 通过：日模式普通上下滚动只浏览时间轴；到达 00:00 后继续下拉切换前一天，到达 24:00 后继续上拉切换后一天，顶部日期同步更新。
- 通过：周模式纵向上滑进入下一周、下滑进入上一周。
- 通过：月模式纵向上滑进入下一月、下滑进入上一月。
- 通过：年模式纵向上滑进入下一年、下滑进入上一年。
- 通过：各模式仍保留既有横向跨度手势，并使用主方向判断避免横纵手势误触。

### 数据与流畅性

- 通过：当前日期范围加载完成后并发预取前后各三个相邻范围。
- 通过：切换跨度时优先使用内存缓存即时更新界面，同时在后台刷新数据。
- 通过：缓存按日、周、月、年规范化键管理，并限制保留当前范围前后各三个跨度，避免长期增长。

### 自动化证据

- `TimiaUITests/testCalendarBlankCreateAndVerticalRangeNavigation`：通过
- `TimiaUITests/testScheduleRedesignModes`：通过
- 验证内容：日/周/月空白区域新建、日模式 00:00/24:00 边界换日、周/月/年纵向切换、既有四视图与 Todo 模式回归
- 结果包：`/tmp/timia-calendar-interactions-final-20260730-1520.xcresult`

final result: passed

## 月模式卡顿、默认定位与三级预取（2026-07-30）

- 通过：月视图结构窗口由一次性创建 2,401 个月缩减为惰性加载的当前月前后各 60 个月，消除点击 M 后的主线程长时间计算。
- 通过：日期键、月份键和月标题改用日历组件直接生成，避免每个日期单元重复创建 `DateFormatter`。
- 通过：从底部 D/W/M/Y 选择器进入月或年模式时，始终以当天所在月份或年份为初始滚动锚点。
- 通过：年模式不再使用可能滞后的接口 `year` 字段决定初始位置，只使用当前 `selectedDate`。
- 通过：月、年视图完成初始程序化定位前不响应可见区偏移；年视图仅在用户真实拖动后更新顶部年份。
- 通过：每次加载当前范围时并发预取前后各 3 个单位，并仅保留当前范围在内的 7 份缓存。
- 示例：当前为 2026 年 7 月时，月模式缓存 2026 年 4 月至 10 月；当前为 2026 年时，年模式缓存 2023 年至 2029 年。
- 通过：月模式日期空白区域始终保留独立点击层，有任务时仍可点击剩余空白创建全天任务。

### 自动化证据

- `TimiaUITests/testCalendarBlankCreateAndVerticalRangeNavigation`：通过
- `TimiaUITests/testScheduleRedesignModes`：通过
- 验证内容：月模式及时进入、默认回到 2026 年 7 月、月空白区域新建、连续滚动到 8 月、年模式默认回到 2026 年并连续滚动到 2027 年、四视图与 Todo 回归
- 交互结果包：`/tmp/timia-month-prefetch-scroll-guard-20260730-1915.xcresult`
- 模式结果包：`/tmp/timia-month-prefetch-redesign-20260730-1930.xcresult`

final result: passed

## 日模式连续拼接与日期同步（2026-07-31）

- 通过：日模式由单日时间轴边界换日改为纵向惰性日期序列，前一天 24:00 后自然衔接后一天。
- 通过：每个日期区段在 00:00 上方显示 `yyyy年MM月dd日` 小字，并保留该日期的全天任务行。
- 通过：首次进入采用两阶段定位，先生成当天区段，再滚动到当前时间附近，避免惰性列表定位到较早日期。
- 通过：滚动跨过日期区段顶部时，同步更新固定日期条的日期、星期、高亮状态及顶部年月。
- 通过：点击顶部日期仍可直接定位到对应日期；由连续滚动触发的日期更新不会产生二次跳动。
- 通过：每个日期的时间格仍支持按 15 分钟粒度点击创建任务，当前时间线仅在当天区段显示。
- 通过：沿用日模式当前日期前后各 3 天并发预取与 7 份范围缓存策略。

### 自动化证据

- `TimiaUITests/testCalendarBlankCreateAndVerticalRangeNavigation`：通过
- `TimiaUITests/testScheduleRedesignModes`：通过
- 验证内容：日模式空白时间新建、连续滚动进入前一天、日期区段标签、顶部日期和星期同步、返回当天、周/月/年及 Todo 回归
- 交互结果包：`/tmp/timia-day-continuous-settled-20260731-1030.xcresult`
- 模式结果包：`/tmp/timia-day-continuous-modes-20260731-1045.xcresult`

final result: passed

## 日周日期与全天标尺对齐（2026-07-31）

- 通过：日模式日期文案、周模式周序号文案及“全天”统一从左侧 8 pt 起始。
- 通过：“全天”严格约束在 48 pt 时间标尺列内，移除原先额外的右侧 7 pt 占位。
- 通过：日期、“全天”和 `00:00` 等时间刻度形成统一的视觉左边缘。
- 通过：任务网格仍从 48 pt 处开始，未因文案调整产生横向位移。
- 交互结果包：`/tmp/timia-axis-label-alignment-20260731-1330.xcresult`

final result: passed
