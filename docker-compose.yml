services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: timia
      POSTGRES_PASSWORD: timia
      POSTGRES_DB: timia
    ports:
      - "5432:5432"
    volumes:
      - timia_pgdata:/var/lib/postgresql/data

volumes:
  timia_pgdata:
