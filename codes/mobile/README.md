# Timia Mobile

移动端代码按平台独立维护，共用 `codes/core-service/openapi.json` 作为后端接口契约。

- `ios/`：原生 SwiftUI 客户端，正在开发。
- `android/`：目录已预留，当前不开发。

iOS 当前范围包括认证、我的日程、任务、评论、工作空间、项目、成员、活动记录和最近讨论。数据分析、代码文档、数据库工具不在 iOS 开发范围内。

详细启动方式见 [`ios/README.md`](ios/README.md)。
